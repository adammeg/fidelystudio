"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function CampaignControls({ slug, state, failed }: { slug: string; state: string; failed: number }) { const router = useRouter(); const [busy, setBusy] = useState(false); async function act(action: string) { if ((action === "cancel" || action === "retry") && !confirm(`${action === "cancel" ? "Cancel campaign" : "Retry failed messages"}?`)) return; setBusy(true); const response = await fetch(`/api/studio/campaigns/${encodeURIComponent(slug)}/${action}`, { method: "POST" }); setBusy(false); if (response.ok) router.refresh(); } return <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginTop: 10 }}>{state === "sending" && <button className="btn btn-secondary btn-sm" disabled={busy} onClick={() => act("pause")}>Pause</button>}{state === "paused" && <button className="btn btn-primary btn-sm" disabled={busy} onClick={() => act("resume")}>Resume</button>}{!["cancelled", "completed"].includes(state) && <button className="btn btn-secondary btn-sm" disabled={busy} onClick={() => act("cancel")}>Cancel</button>}{failed > 0 && <button className="btn btn-secondary btn-sm" disabled={busy} onClick={() => act("retry")}>Retry {failed} failed</button>}</div>; }
